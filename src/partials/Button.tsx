import { StyledButtonPrimary } from "../styles/button.styles";

const ButtonPrimary = () => {
  return (
    <StyledButtonPrimary variant="contained" color="secondary">
      Shop Now
    </StyledButtonPrimary>
  );
};

export default ButtonPrimary;
